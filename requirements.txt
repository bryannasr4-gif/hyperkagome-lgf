# Reproduce every certification with CPython 3.10+ (no SageMath required).
# Verified working with the versions in parentheses.
sympy>=1.12      # (tested 1.14.0)  -- uses Poly.invert, ff, as_leading_term
numpy>=1.24      # (tested 2.5.0)    -- Bloch-Hamiltonian and quadrature cross-checks only
